# Archived HTML

These files are retained for historical reference and are not intended to be active production routes.

- `index-wordpress.html` is the original WordPress export snapshot.
- `example-custom-header.html` is a demo page used while testing component customization.
